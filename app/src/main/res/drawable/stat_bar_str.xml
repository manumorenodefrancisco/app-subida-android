<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background track -->
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="@color/negriazul_podom3" />
            <corners android:radius="2dp" />
        </shape>
    </item>
    
    <!-- Progress fill -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/stat_str1"
                    android:endColor="@color/stat_str2"
                    android:angle="90" />
                <corners android:radius="2dp" />
            </shape>
        </clip>
    </item>
</layer-list>
